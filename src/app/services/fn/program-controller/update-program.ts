/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Program } from '../../models/program';
import { UpdateProgramDto } from '../../models/update-program-dto';

export interface UpdateProgram$Params {
      body: UpdateProgramDto
}

export function updateProgram(http: HttpClient, rootUrl: string, params: UpdateProgram$Params, context?: HttpContext): Observable<StrictHttpResponse<Program>> {
  const rb = new RequestBuilder(rootUrl, updateProgram.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Program>;
    })
  );
}

updateProgram.PATH = '/auth/programs/updateProgram';
